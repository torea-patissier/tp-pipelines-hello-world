name: Hello World

on:
  push:
    branches: [main]

jobs:

  stages:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello, world!"

  hello:
    needs: stages
    runs-on: ubuntu-latest
    steps:
      - name: Run Hello World
        run: echo "Hello, world!"

  commit:
    runs-on: ubuntu-latest
    steps:
      - name: Short commit
        run : echo $REFERENCE_COURTE_COMMIT
